services:
  backend:
    build: .
    ports:
      - "8105:8000"
    env_file:
      - .env
    volumes:
      - ./uploads:/app/uploads


networks:
  default:
    name: firecrawl_docling_network
    external: true
